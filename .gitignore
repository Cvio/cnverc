/target/

# Model weights are never committed. Only the engine.toml descriptors are.
*.onnx
*.gguf
*.bin
*.tar.bz2
*.tar.gz
*.zip

# Model archives downloaded during setup (README step 6).
/downloads/

# Runtime output.
/logs/

# Machine-local Claude Code session settings.
.claude/settings.local.json
